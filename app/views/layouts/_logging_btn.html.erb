<%= link_to "#{t('submit.login_submit')}", root_url, :class => "btn btn-primary" %>

<% if withReg %>
  <%= link_to "#{t('submit.registration_submit')}", new_user_url, :class => "btn btn-link" %>
<% end %>
